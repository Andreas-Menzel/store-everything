# Generated: rewritten by `make openapi` / `make corpus`, never edited by hand.
# Reformatting a fixture would break the hash recorded for it in the manifest, and
# reformatting an exported contract would make its drift check fail on every run.
openapi.json
openapi-extractor.json
corpus/fixtures/
packages/api-client/src/generated/

# Build output and caches
dist/
coverage/
storybook-static/
playwright-report/
test-results/
pnpm-lock.yaml

# Owned by other toolchains
server/
LICENSE

# Documentation is hand-formatted; reflowing it would bury real changes in noise.
*.md
